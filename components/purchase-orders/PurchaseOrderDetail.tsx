import { FC } from 'react'
import { PurchaseOrder, PurchaseOrderItem } from '@prisma/client'

interface PurchaseOrderDetailProps {
  id: string
}

export const PurchaseOrderDetail: FC<PurchaseOrderDetailProps> = ({ id }) => {
  // コンポーネントの実装
  return (
    <div>
      {/* 実装内容 */}
    </div>
  )
} 